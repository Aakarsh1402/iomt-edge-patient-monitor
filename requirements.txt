# Everything runs on CPU; no GPU, broker or hardware needed.
#   pip install -r requirements.txt
# On a machine without a display use opencv-python-headless instead of opencv-python.

# Models
tensorflow>=2.16          # EHR risk engine + arrhythmia model (TFLite runtime included); use tensorflow-cpu for a smaller install
torch>=2.1                # IMU motion classifier, vision classifier (CPU wheels: --index-url https://download.pytorch.org/whl/cpu)
torchvision>=0.16
scikit-learn>=1.3         # metrics, EHR scaler
scipy>=1.10               # ECG baseline filter, TFLite output-order recovery

# Data
numpy>=1.26
pandas>=2.1
joblib>=1.3

# Vision
opencv-python>=4.8
pillow>=10.0

# Live pipeline (MQTT client; the broker is fusion/mini_broker.py or Mosquitto)
paho-mqtt>=2.0

# Plots + tests
matplotlib>=3.8
pytest>=7.0

# Only for retraining the EHR model from a FHIR corpus / the original Colab notebook
# pyarrow>=14.0
# wfdb>=4.1
# neurokit2>=0.2
# plotly>=5.18
